//Tipo Primitivo

const mensagem = 'minha primeira mensagem'

//Tipo Objeto

const outraMensagem = new String('minha segunda mesagem') //instancia a String como do tipo objeto

outraMensagem.length // diz o tamanho

outraMensagem[2] //retorna o indice 2

mensagem.includes('primeira')

mensagem.includes('vermelho') // verifica se está dentro da string

mensagem.startsWith('minha') // verifica se é a parte inicial

mensagem.endsWith('mensagem')// verifica se é a parte final

outraMensagem.indexOf('segunda') //dá o índice em que começa

mensagem.replace('minha', 'sua') //substitui 

mensagem.trim() //remove espaços excedentes

mensagem.split(' ') //reparte a string toda vez que encontra um espaço vazio

